import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // basePath: '/enzouro',
  // assetPrefix: '/enzouro',
  output: 'export',
  // images: {
  //   unoptimized: true
  // }
};

export default nextConfig;
